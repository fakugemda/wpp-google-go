package constants

const ONLY_SENDER = "whatsapp:+5493454127524"

// Commands
const (
	COMMAND_MAIL_PREFIX = "mail a"
	COMMAND_CANCEL      = "cancelar"
	COMMAND_CONFIRM     = "si"
	MAIL_PREFIX_LENGTH  = 7
	DEFAULT_SUBJECT     = "Mail enviado desde WhatsApp"
)

// Interactive buttons
const (
	BUTTON_CONFIRM_ID = "confirm_send"
)

const (
	GEMINI_MODEL = "gemini-2.5-flash-lite"
)
